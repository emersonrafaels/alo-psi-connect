import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import type { BodyMapLayer } from "../types";

const OPTIONS = [{ id: "less", label: "Menos intenso" }, { id: "same", label: "Igual" }, { id: "more", label: "Mais intenso" }, { id: "unsure", label: "Não sei" }] as const;
export const BodyCheckout = ({ layers, onChange, onBack, onNext }: { layers: BodyMapLayer[]; onChange: (layerId: string, after: "less" | "same" | "more" | "unsure") => void; onBack: () => void; onNext: () => void }) => <Card className="border-border/70 shadow-sm"><CardContent className="space-y-6 p-5 sm:p-8"><div><p className="text-xs font-semibold uppercase text-primary">Regular · check-out corporal</p><h2 className="mt-2 text-2xl font-semibold">E no corpo, como ficou?</h2><p className="mt-2 text-sm text-muted-foreground">Não é necessário haver melhora. “Igual”, “mais intenso” e “não sei” também são respostas válidas.</p></div><div className="space-y-3">{layers.map((layer) => <div key={layer.id} className="rounded-xl border border-border p-4"><div className="flex items-center gap-2 font-semibold"><span className="h-3 w-3 rounded-full" style={{ backgroundColor: layer.color }}/>{layer.label}</div><div className="mt-3 flex flex-wrap gap-2">{OPTIONS.map((option) => <Button key={option.id} variant={layer.after === option.id ? "default" : "outline"} size="sm" onClick={() => onChange(layer.id, option.id)}>{option.label}</Button>)}</div></div>)}</div><p className="text-xs text-muted-foreground">Essa comparação pertence ao mesmo check-in e não prova que a prática causou uma mudança.</p><div className="flex flex-wrap justify-between gap-2"><Button variant="outline" onClick={onBack}>Voltar à prática</Button><Button onClick={onNext}>Continuar para agir</Button></div></CardContent></Card>;